# films_map
This program reads file locations.list and creates a html map with locations of films, choosed by user.
This program requires additional libraries such as geopy and folium. You can install them through command line:
>pip3 install folium<br/>
>pip3 install geopy
